# requirements-logging.txt - Additional dependencies for enhanced logging system

# Core logging dependencies (included in Python standard library)
# - logging
# - logging.handlers
# - json
# - threading
# - asyncio

# Enhanced terminal display (optional but recommended)
rich>=13.0.0

# HTTP client for webhook notifications (optional)
aiohttp>=3.8.0

# Email functionality (included in Python standard library)
# - smtplib
# - email.mime.text
# - email.mime.multipart

# For development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# For log parsing and analysis (optional)
pandas>=1.5.0  # For advanced log analysis
matplotlib>=3.6.0  # For generating charts from metrics

# Alternative installations:
# pip install rich aiohttp
# pip install -r requirements-logging.txt